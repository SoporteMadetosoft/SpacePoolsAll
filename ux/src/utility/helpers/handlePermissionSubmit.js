

export const deconstructPermisos = (obj) => {
    // console.log(obj)
    // const newObject = new Object
    // Object.entries(obj).map((el) => {
    //     Object.entries(el[1]).map((elem) => {
    //         if (elem[1] === true) {
    //             newObject[el[0]] += `${elem[0]},`
    //             //console.log(elem)

    //         }
    //     })
    // })

    // return {}
}